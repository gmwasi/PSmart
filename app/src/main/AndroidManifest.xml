<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.kenyahmis.psmart">
<uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Trial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CardReaderActivity"
            android:label="@string/title_activity_card_reader"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter
                android:label="Write P-Smart Card"
                android:priority="1">
                <category android:name="android.intent.category.DEFAULT" />

                <action android:name="org.kenyahmis.psmart.ACTION_READ_DATA" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity android:name=".DeviceScanActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ReaderSettingsActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ReaderStatusActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".MainActivity" />
    </application>

</manifest>