<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.kenyahmis.psmart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CardReaderActivity"
            android:label="@string/title_activity_card_reader"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter
                android:label="Write P-Smart Card"
                android:priority="1">
                <category android:name="android.intent.category.DEFAULT" />

                <action android:name="org.kenyahmis.psmart.WRITE_DATA" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity android:name=".DeviceScanActivity" />
        <activity android:name=".ReaderSettingsActivity" />
        <activity android:name=".ReaderStatusActivity"></activity>
    </application>

</manifest>